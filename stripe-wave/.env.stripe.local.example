# Stripe API — local only (gitignored via .env*.local)
# Create a Restricted key: https://dashboard.stripe.com/apikeys
# Permissions (Read): Balance, Balance transaction charges, Charges, Payouts, Payment Intents
STRIPE_SECRET_KEY=rk_live_REPLACE_ME

# Optional: only sync BTs on/after this date (YYYY-MM-DD)
# STRIPE_SYNC_SINCE=2026-07-01
