/**
 * Bootstrap entry — loads the parking lot orchestrator (UI, events, init).
 * Extracted modules: `render/`, `features/`, `app/orchestrator.js`.
 * @see docs/MODULAR_REFACTOR_CHECKLIST.md
 */
import './app/orchestrator.js';
