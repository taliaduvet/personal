// Copy this file to config.js and fill in your Supabase credentials.
// Get them from: Supabase Dashboard → Project Settings → API
// Use var (not const) so values live on globalThis and the ES module app can read them.

var SUPABASE_URL = 'https://feodlwvjcayfgujkxcxm.supabase.co';
var SUPABASE_ANON_KEY = 'your-hub-anon-key-here';
// Where magic links land after email verify (must be in Supabase Auth → Redirect URLs).
var AUTH_REDIRECT_URL = 'https://taliaduvet.github.io/personal/parking-lot-app';
