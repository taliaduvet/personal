import { ScreenStub } from "@/components/ScreenStub";

export default function TodayPage() {
  return (
    <ScreenStub
      title="Today"
      blurb="One thing at a time — the calm, focused view of what to do right now."
      phase="M2"
    />
  );
}
