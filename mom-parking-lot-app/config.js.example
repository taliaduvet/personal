// Copy this file to config.js and fill in your Supabase credentials.
// Get them from: Supabase Dashboard → Project Settings → API
// Use var (not const) so values live on globalThis and the ES module app can read them.

var SUPABASE_URL = 'https://your-project-id.supabase.co';
var SUPABASE_ANON_KEY = 'your-anon-key-here';
